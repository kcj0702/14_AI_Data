# =========================================================
# 컨테이너 자료형 - [5] Set 자료형
# 
# - set 자료형과 연산자
# =========================================================
# ---------------------------------------------------------
# 예시 set 생성
# ---------------------------------------------------------
data = {1, 1, 2, 1, 3, 2, 5, 10, 11, 12}
print(f"data : {len(data)}개, {type(data)}, {data}")

# ---------------------------------------------------------
# 연산자 : 산술 연산자 덧셈 + => set + set  ★ 미지원 기능
#                                          원한다면 형변환 필요
# ---------------------------------------------------------
# newdata = data + {23, 25, 33, 37}
# print(f"data : {len(newdata)}개, {type(newdata)}, {newdata}")

# ---------------------------------------------------------
# 연산자 : 산술 연산자 곱셈 * => set + set  ★ 미지원 기능
#                                          원한다면 형변환 필요
# ---------------------------------------------------------
# newdata = data * 2
# print(f"data : {len(newdata)}개, {type(newdata)}, {newdata}")

# ---------------------------------------------------------
# 연산자 : 멤버 연산자 => 데이터 in 집합     : 존재하면 True
#                      데이터 not in 집합 : 존재하지 않으면 True
# ---------------------------------------------------------
print(f"11 in data     : {11 in data}")
print(f"11 not in data : {11 not in data}")
